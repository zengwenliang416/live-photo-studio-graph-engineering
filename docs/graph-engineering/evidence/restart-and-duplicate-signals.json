{
  "schema_version": 1,
  "assertion_id": "A1",
  "status": "passed",
  "captured_at": "2026-08-23T18:15:46+08:00",
  "environment": {
    "node": "v22.19.0",
    "pnpm": "10.20.0",
    "postgres": "16.11 (Homebrew)",
    "provider": "mock"
  },
  "commands": [
    {
      "command": "RUN_PG_TESTS=1 pnpm --filter @live-photo-studio/orchestrator test",
      "result": "passed",
      "observed": "11 tests passed, including restart at every interrupt, duplicate completion, duplicate START, wrong correlation, late cancellation signal, stale recovery, concurrent duplicate signals, consumed-marker crash recovery, and old graph version resolution."
    },
    {
      "command": "pnpm --filter @live-photo-studio/orchestrator build",
      "result": "passed",
      "observed": "TypeScript build completed with exit code 0."
    },
    {
      "command": "pnpm --filter @live-photo-studio/orchestrator demo",
      "result": "passed",
      "observed": "The demo printed WAITING_GENERATION, REVIEW_ANCHOR, WAITING_RENDER, and COMPLETED in order."
    }
  ],
  "observed_invariants": [
    "Duplicate signal delivery increments the duplicate counter without creating a second transition.",
    "A signal persisted before a resume crash is recovered once after the visibility timeout.",
    "A resume crash before the consumed marker does not duplicate effects, tasks, or Outbox rows.",
    "Late signals after cancellation do not reopen a terminal run.",
    "A run keeps its original graph version after a newer graph version is registered."
  ],
  "limitations": [
    "These checks run on Node 22 although the repository declares Node >=24.",
    "The live Redis/BullMQ publication path remains blocked by local Redis authentication."
  ]
}
